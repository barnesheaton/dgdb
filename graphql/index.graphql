type Game {
  id: ID!
  name: String!
  description: String
  min_players: Int!
  max_players: Int
}

type RootQuery {
  getGame(id: ID!): Game
  getGames(filter: JSON): [Game]
}

schema {
  query: RootQuery
}

scalar JSON
