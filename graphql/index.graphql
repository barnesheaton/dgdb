type Game {
  name: String
  description: String
  min_players: Int
  max_players: Int
}

type RootQuery {
  game(name: String, description: String, players: Int): Game
  games: [Game]
}

schema {
  query: RootQuery
}
